export interface AppUsers {
  appId: string;
  userId: string;
  parentId: string;
  parentReferralCode: string;
  parentDiscount: number;
  referralCode: string;
  pointsEarned: number;
  rate: number;
}
