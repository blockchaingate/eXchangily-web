/*const mongoose = require('mongoose');

module.exports = mongoose.model('SmartContract', {
  name : String,
  address : String,
  abi: JSON,
  sourceCode: String,
  byteCode: String,
  isFeatured: Boolean
});
*/
